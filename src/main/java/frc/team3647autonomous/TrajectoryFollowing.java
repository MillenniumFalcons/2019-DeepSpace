package frc.team3647autonomous;

import jaci.pathfinder.*;

public class TrajectoryFollowing
{
    // Trajectory trajectory = new Trajectory(segments);

    double timeStep = .02;
    double maxVel = 1.7;
    double accel = 2.0;
    double jerk = 60.0;

    // Trajectory.Config config = new Trajector.Config(Trajectory.FitMethod.HERMITE_QUINTIC, Trajectory.Config.SAMPLES_HIGH, );

}